/**
 * This config file is ONLY a template for the actual config file. To create
 * your own configuration file, copy this file and remove the ".template" from
 * the name of the file. That file will be ignored by git, in order to prevent
 * conflicts between configurations between developers.
 *
 * The values in this file are for example purposes ONLY. You will have to
 * modify some of the values when you run the app locally.
 */

const api = {

}

module.exports = api
